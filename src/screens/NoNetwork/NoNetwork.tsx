import { ReactElement } from 'react';
import { View } from 'react-native';
import Text from '@core/Text';

const NoNetwork = (): ReactElement => {
  return (
    <View>
      <Text>NoNetwork</Text>
    </View>
  );
};

export default NoNetwork;
